[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m 'not integration_postgres'
markers =
    integration_postgres: testes de integracao que exigem PostgreSQL real em Docker
timeout = 20
timeout_method = thread
