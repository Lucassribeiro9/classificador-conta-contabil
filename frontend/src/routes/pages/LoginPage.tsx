import { PagePlaceholder } from "./PagePlaceholder";

export function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Tela publica para iniciar sessao JWT antes de acessar as rotas internas."
    />
  );
}
