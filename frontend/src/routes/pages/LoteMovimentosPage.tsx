import { PagePlaceholder } from "./PagePlaceholder";

export function LoteMovimentosPage() {
  return (
    <PagePlaceholder
      title="Lote de Movimentos"
      description="Rota base para listar movimentos, filtros e acoes operacionais do lote."
    />
  );
}
